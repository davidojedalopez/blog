{# <div class="border rounded border-secondary bg-foreground pt-2 shadow-md">  
  <h3 class="px-3 pb-3 truncate">{{ post.data.title }}</h3>
  <p class="px-4">{{ post.data.description }}</p>
  <time class="px-4" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>  
  {% if post.data.cover %}
    <img src="{{ post.data.cover}}" />
  {% endif %}
</div> #}

<li class="py-3 hover:bg-primary rounded px-3 group cursor-pointer">
  <a class="no-underline" href="{{ post.url | url }}">
    <h3 class="group-hover:text-black">{{ post.data.title }} <span class="text-xs">🔗</span> </h3>
    <p class="pb-1">{{ post.data.description}}</p>
    <div class="space-x-5">
      <time class="text-sm text-secondary opacity-50"  datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>        
      <span class="text-sm text-secondary opacity-75">🧡 {{ post.data.positive_reactions_count }}</span>
      <span class="text-sm text-secondary opacity-75">💬 {{ post.data.comments_count }}</span>
      <span class="text-sm text-secondary opacity-75">👀 {{ post.data.page_views_count }}</span>
    </div>
  </a>
</li>