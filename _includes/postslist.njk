<div class="pt-4 pb-2">
  <p>Most recent</p>
</div>

{# <div class="grid grid-cols-1 sm:grid-cols-2 sm:col-gap-6 row-gap-6 px-2">  
  {% for post in postsList | reverse %}
    {% include "postitem.njk" %}
  {% endfor %}
</div> #}

<div class="flex">
  <ul class="max-w-xl pl-1 list-none divide-opacity-50 divide-primary flex-auto">
    {% for post in postsList | reverse %}
      {% include "postitem.njk" %}
    {% endfor %}
  </ul>

  <svg class="flex-auto sm:hidden md:inline" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">
    <path fill="#586674" d="M43.8,-40.9C59.5,-28.1,76.8,-14.1,80.6,3.8C84.5,21.7,74.8,43.5,59.1,51.6C43.5,59.7,21.7,54.2,5.4,48.9C-11,43.5,-21.9,38.2,-35.8,30.1C-49.7,21.9,-66.4,11,-66.7,-0.2C-66.9,-11.4,-50.6,-22.9,-36.7,-35.7C-22.9,-48.5,-11.4,-62.7,1.3,-64C14.1,-65.3,28.1,-53.8,43.8,-40.9Z" transform="translate(100 100)" />
  </svg>
</div>


{# <ol class="" style="counter-reset: start-from {{ (postsListCount or postsList.length) + 1 }}">
{% for post in postsList | reverse %}
  <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
    <a href="{{ post.url | url }}" class="postlist-link">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a>
    <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
    {% for tag in post.data.tags %}
      {%- if collections.tagList.indexOf(tag) != -1 -%}
      {% set tagUrl %}/tags/{{ tag }}/{% endset %}
      <a href="{{ tagUrl | url }}" class="tag">{{ tag }}</a>
      {%- endif -%}
    {% endfor %}
  </li>
{% endfor %}
</ol> #}
